/**
 * this route is responsible for adding new venders to a vender station 
 */

const router = require('express').Router()

const venderSetup = require('../../controllers/venderSetup')


router.post('/', (req, res) => {

})